import type { ReactNode } from 'react';

export default function DriverJobsLayout({ children }: { children: ReactNode }) {
  return <div className="driver-reference-page driver-reference-jobs">{children}</div>;
}
