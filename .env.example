# Example environment configuration for Prisma.
# Replace these values with the real Neon DB credentials.

# Neon PostgreSQL Database URL
# Format: postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require
# Example: postgresql://user:pass@ep-cool-name-123456.region.aws.neon.tech/xdrive?sslmode=require
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"

# Optional: Direct URL for Neon (if using connection pooling)
# DIRECT_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
