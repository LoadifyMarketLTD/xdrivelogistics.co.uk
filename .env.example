# copy to .env and edit
PORT=3000
DATABASE_URL=postgres://username:password@localhost:5432/xdrive
APP_BASE_URL=http://localhost:3000                # used to build verification link (replace with frontend domain in production)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-pass
EMAIL_FROM="XDrive <no-reply@yourdomain.com>"
CORS_ORIGIN=http://localhost:8000                 # frontend origin during development
BCRYPT_ROUNDS=12
VERIFY_TOKEN_EXPIRES_MIN=60
