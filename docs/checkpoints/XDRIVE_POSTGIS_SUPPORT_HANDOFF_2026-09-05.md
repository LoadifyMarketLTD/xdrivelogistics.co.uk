# XDrive Production PostGIS Support-assisted relocation handoff — 2026-09-05

## Gate status

- Repository: `LoadifyMarketLTD/xdrivelogistics.co.uk`
- PR: `#502` (`go-live-postgis-relocation-bridge-20260905`)
- PR must remain **DRAFT / NOT MERGED** until the post-relocation authenticated cross-company write-path smoke passes.
- GitHub Actions are **not** part of this gate.
- Production project ref: `jqxlauexhkonixtjvljw`
- Target PostGIS schema: `extensions`

## Production state verified before Support handoff

- PostGIS version: `3.3.7`
- Current PostGIS schema: `public`
- Extension owner: `supabase_admin`
- Installed PostGIS is non-relocatable (`relocatable = false`)
- `public.spatial_ref_sys` owner: `supabase_admin`
- `public.spatial_ref_sys` RLS: disabled
- XDrive geography dependency: `public.driver_locations.location`
- Anonymous/authenticated roles do not have `CREATE` on either `public` or `extensions`.

## Runtime compatibility bridge — APPLIED TO PRODUCTION

Canonical repository migration:

`20260905003500_prepare_postgis_schema_relocation_bridge.sql`

Hosted Production ledger version generated by the Supabase migration endpoint:

`20260905012522_prepare_postgis_schema_relocation_bridge`

Repository hosted-version reconciliation alias:

`20260905012522_prepare_postgis_schema_relocation_bridge.sql`

Post-apply verification:

- `public.fn_sync_driver_location_coordinates()` has `search_path=public, extensions, pg_catalog`;
- `public.fn_enqueue_driver_load_alerts_for_job(uuid, uuid)` has `search_path=public, extensions, pg_catalog`;
- PostGIS itself remains in `public`;
- `spatial_ref_sys` remains unchanged;
- no business rows were mutated by the bridge.

## Request to Supabase Support

Please perform a **Support-assisted relocation of the existing non-relocatable PostGIS extension from `public` to `extensions`** for Production project `jqxlauexhkonixtjvljw`, using the supported privileged procedure for an existing managed PostGIS installation.

Constraints:

1. Do **not** use `DROP EXTENSION postgis CASCADE`.
2. Do **not** drop or recreate XDrive business tables or spatial data.
3. Do **not** require application-side direct mutation of `pg_extension`.
4. Preserve `public.driver_locations.location` data and type integrity.
5. Do not mutate unrelated business data.
6. Keep PostGIS at a supported version unless Support determines an extension update is required as part of its documented relocation procedure.

Please confirm after the intervention:

- PostGIS extension schema is `extensions`;
- installed PostGIS version;
- extension relocatable flag/state after the procedure;
- location/schema and owner of `spatial_ref_sys`;
- whether `spatial_ref_sys` still requires an RLS/security-advisor exception after relocation;
- `public.driver_locations.location` remains valid and queryable;
- no unexpected dependent objects were dropped or recreated.

Official Supabase guidance used for this gate: `https://supabase.com/docs/guides/database/extensions/postgis` (existing non-relocatable PostGIS relocation via Supabase Support).

## Mandatory post-Support verification — do not skip

Before any merge or GO-LIVE verdict, verify directly in Production:

1. `pg_extension.extnamespace` for `postgis` resolves to `extensions`.
2. PostGIS version and installed state are healthy.
3. `spatial_ref_sys` is no longer an exposed `public` table, or the remaining state is explicitly documented/accepted by Support.
4. `public.driver_locations.location` remains the expected geography column and existing rows remain valid.
5. Both XDrive PostGIS runtime functions resolve successfully after relocation.
6. Supabase security advisor is re-checked for the PostGIS / `spatial_ref_sys` finding.
7. Run the final authenticated cross-company write-path smoke under a real non-platform-owner tenant identity with all probe writes transactionally rolled back.

## Final authenticated write-path smoke acceptance criteria

Tenant A authenticated session must prove all of the following in one rollback-forced probe:

- an allowed same-tenant write path succeeds on a disposable/reversible field or temporary probe row;
- cross-company update of tenant B job is denied/no-op;
- cross-company update of tenant B invoice is denied/no-op;
- insertion of a job bound to tenant B from tenant A is denied;
- tenant A cannot gain membership/authority in tenant B through the tested write paths;
- all test mutations are rolled back and no probe rows remain;
- no migration-history entry is created by the smoke.

Only after all checks pass may PR #502 leave DRAFT and proceed to the final merge decision.
