'use client';

import ParticipantMessagesPage from '../../components/workspace/ParticipantMessagesPage';

export default function AdminMessagesPage() {
  return (
    <ParticipantMessagesPage
      eyebrow="Operations communication"
      description="Continue verified participant conversations linked to company transport activity. Messaging remains participant-scoped and separate from operational alerts."
    />
  );
}
