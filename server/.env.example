# Server Configuration
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://xdrive:xdrive123@localhost:5432/xdrive_db

# CORS
CORS_ORIGIN=http://localhost:3000

# Application
APP_BASE_URL=http://localhost:3000

# Email (SMTP)
# If not configured, verification links will be logged to console instead
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@xdrivelogistics.co.uk

# Security
BCRYPT_ROUNDS=10
VERIFY_TOKEN_EXPIRES_MIN=60
JWT_SECRET=your-secret-key-change-in-production
