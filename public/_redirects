# Netlify automatically handles www to apex domain redirects based on primary domain configuration
# Primary domain is dannycourierltd.co.uk (configured in Netlify dashboard)

# SPA redirect rule - redirect all routes to index.html for client-side routing
/*    /index.html   200
