# Fleet Driver staging validation trigger

This inert file exists only to trigger the exact-head Fleet Driver invitation workflow after the physical schema and runtime guards were reconciled. It is not imported by the application.

Current rerun reason: disposable P0 staging schema verified successfully at 204/204 migrations after excluding environment-specific live-account reconciliation from disposable manifests.
