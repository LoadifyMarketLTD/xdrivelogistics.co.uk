# Production environment example — copy to .env (server) and completați valorile reale.
# NOTE: folositi secret manager (Vercel/Netlify/Render/GCP Secret Manager/AWS Secrets Manager) in productie.

# App
PORT=3000
NODE_ENV=production
APP_BASE_URL=https://app.taudomeniu.com    # frontend public URL
CORS_ORIGIN=https://app.taudomeniu.com

# Database (Postgres). Exemple:
# - Neon / Supabase: postgresql://user:pass@dbhost:5432/dbname
# - Heroku: postgres://user:pass@ec2-...amazonaws.com:5432/dbname
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# SMTP (foloseste unul din exemplele de mai jos)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM="XDrive <no-reply@taudomeniu.com>"

# Auth / security
BCRYPT_ROUNDS=12
VERIFY_TOKEN_EXPIRES_MIN=60

# Optional / Integrations (secret, nu expune client-side)
SUPABASE_SERVICE_ROLE_KEY=
STRIPE_SECRET_KEY=
SENTRY_DSN=

# Operational
LOG_LEVEL=info
# Feature flags
FEATURE_X=true
