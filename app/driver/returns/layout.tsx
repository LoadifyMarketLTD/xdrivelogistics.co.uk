import type { ReactNode } from 'react';
import './returns-exchange.css';

export default function DriverReturnsLayout({ children }: { children: ReactNode }) {
  return <div className="driver-returns-exchange">{children}</div>;
}
