# Backend Server Configuration (see server/.env.example for backend-specific vars)
# This file is for root-level environment variables

# Next.js Configuration (if using Next.js frontend)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Supabase (if still using for other features)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# For backend API server, see server/.env.example
